# CFDI Processor - runtime dependencies
# Install with: pip install -r requirements.txt
#
# Parsing uses xml.etree.ElementTree (Python stdlib) - no dependency needed.

pandas>=2.0
openpyxl>=3.1

# GUI (Step 1). PySide6 = LGPL -> libre para uso comercial/cerrado.
# El front-end de consola (main.py) usa Tkinter (stdlib) y no requiere esto.
PySide6>=6.8
